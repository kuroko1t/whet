tax.py contains four nearly-identical functions (calc_with_tax_us, calc_with_tax_eu, calc_with_tax_jp, calc_with_tax_uk). Refactor them so the validation logic ("price must be non-negative") and the rounding logic appear at most once in the file (extract a helper). The four public functions must keep their existing names, signatures, and behaviour: each returns the same numeric result as before for valid prices and still raises ValueError for negative prices.
